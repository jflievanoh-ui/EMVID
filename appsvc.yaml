version: 1

pre-build:
  - python -m pip install --upgrade pip
  - python -m pip install -r requirements.txt

post-build: |
  echo "Post-build steps completed"

run: gunicorn main:app --workers 4 --bind 0.0.0.0:$PORT
